#include </home/aissy/Documents/exploits/bitcoin/src/warnings.h>
//#include </home/aissy/Documents/exploits/bitcoin/src/warnings.h>
#include <stdio.h>

int f(int x) {
    return x+1;
}

int* Xim(int x) {
    int* b =  (int*)(x);
    int* g1 = {};
    for (int i; i < x;i++) {
            g1[i] = f(i); 
    }
    //std::vector<x;
    if (x > 10) {
       return g1 ;
    }
    int* fx = Xim(x+1); 
    return fx,g1;
    }

int main() {
    int* Xim(int* x); 
    bilingual_str GetWarnings(bool n);
    void SetMiscWarning(const bilingual_str SetfLargeWorkInvalidChainFound(bool n));
    struct x;
    bilingual_str Xim(bool n); 
    return 0;

}
